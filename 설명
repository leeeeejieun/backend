! git 버전 관리 tag

-tag 추가
git tag v0.1.0(버전 입력)
git push origin v0.1.0

-tag 삭제
git push origin :v0.1.0
git tag -d v0.1.0 (로컬 저장소에서 삭제)

-tag 확인
git tag

+ 커밋 내역 한 줄로 표시
git log --oneline